name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Debug environment
        run: |
          echo "HOME directory: $HOME"
          echo "Creating .ssh directory..."
          mkdir -p $HOME/.ssh
          ls -la $HOME/.ssh
        shell: bash

      - name: Add SSH key and setup known hosts
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SERVER_IP: ${{ secrets.SERVER_IP }}
        run: |
          echo "Adding SSH key..."
          echo "$SSH_PRIVATE_KEY" > $HOME/.ssh/id_rsa
          chmod 600 $HOME/.ssh/id_rsa
          echo "SSH key added. Listing .ssh directory..."
          ls -la $HOME/.ssh
          echo "Adding known hosts..."
          ssh-keyscan $SERVER_IP >> $HOME/.ssh/known_hosts 2>&1
          echo "Known hosts added. Listing known_hosts file..."
          cat $HOME/.ssh/known_hosts
        shell: bash

      - name: Deploy to server
        env:
          SERVER_IP: ${{ secrets.SERVER_IP }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
        run: |
          echo "Deploying to server..."
          rsync -avz --delete dist/ $SERVER_USER@$SERVER_IP:/var/www/html/dist
          ssh $SERVER_USER@$SERVER_IP 'sudo systemctl restart nginx'
        shell: bash
